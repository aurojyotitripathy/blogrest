app.url=http://localhost:9090/blogapi/
spring.jpa.hibernate.ddl-auto = update
spring.datasource.url = jdbc:mysql://localhost:3306/blogrest?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username = blogrest
spring.datasource.password = Blogrest@707

spring.jpa.show-sql = true

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

app.jwtSecret = secret

# Expiration in milliseconds - 1 Hour
app.jwtExpirationInMs = 3600000


spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS = false
spring.jackson.time-zone = UTC

cors.allowedOrings=*
server.port = 9090
spring.application.name = blogapi
server.servlet.context-path=/blogapi